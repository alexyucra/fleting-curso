# 🚀 Crear primer proyecto Flet con Fleting Framework

```bash
# installe flet y fleting
pip install flet
pip install feting

# inicialize proyecto nombre del pryecto "app"
fleting init app
cd app
fleting run
```